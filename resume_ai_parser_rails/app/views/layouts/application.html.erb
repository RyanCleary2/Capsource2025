<!DOCTYPE html>
<html>
  <head>
    <title>Resume AI Parser - CapSource Style</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              'capsource-orange': '#F7931D',
              'capsource-dark': '#301C2A',
              'capsource-mauve': '#66525f',
              'capsource-purple': '#921245',
              'capsource-red': '#CC3131',
              'capsource-blue': '#3353ea'
            },
            fontFamily: {
              'avenir': ['Avenir-Book', 'Open Sans', 'sans-serif']
            }
          }
        }
      }
    </script>
    <style>
      body {
        font-family: 'Open Sans', 'Avenir-Book', sans-serif;
        line-height: 1.75;
      }

      .capsource-button {
        background-color: #F7931D;
        color: white;
        padding: 15px 30px;
        text-transform: uppercase;
        font-weight: 600;
        letter-spacing: 0.5px;
        border: none;
        cursor: pointer;
        transition: background-color 0.3s ease;
        min-height: 45px;
      }

      .capsource-button:hover {
        background-color: #e8851a;
      }

      .capsource-card {
        background: white;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        border: 1px solid #e5e7eb;
      }

      .skill-tag {
        padding: 8px 16px;
        border-radius: 20px;
        font-size: 0.875rem;
        font-weight: 500;
      }

      .technical-skill {
        background-color: #3353ea;
        color: white;
      }

      .soft-skill {
        background-color: #921245;
        color: white;
      }

      .language-skill {
        background-color: #66525f;
        color: white;
      }
    </style>
  </head>

  <body class="bg-gray-50 font-avenir">
    <!-- CapSource Navigation Header -->
    <nav class="bg-white shadow-sm border-b border-gray-200">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <!-- Logo -->
          <div class="flex items-center">
            <%= image_tag "CapSource_logo.png", alt: "CapSource", class: "h-8 w-auto" %>
          </div>

          <!-- Navigation Links -->
          <div class="hidden md:flex items-center space-x-8">
            <a href="#" class="text-gray-700 hover:text-capsource-orange transition-colors font-medium">EDUCATORS</a>
            <a href="#" class="text-gray-700 hover:text-capsource-orange transition-colors font-medium">STUDENTS</a>
            <a href="#" class="text-gray-700 hover:text-capsource-orange transition-colors font-medium">ORGANIZATIONS</a>
            <div class="relative group">
              <a href="#" class="text-gray-700 hover:text-capsource-orange transition-colors font-medium flex items-center">
                OPPORTUNITIES
                <svg class="ml-1 h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                </svg>
              </a>
            </div>
            <div class="relative group">
              <a href="#" class="text-gray-700 hover:text-capsource-orange transition-colors font-medium flex items-center">
                LEARN MORE
                <svg class="ml-1 h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                </svg>
              </a>
            </div>
          </div>

          <!-- Auth Buttons -->
          <div class="flex items-center space-x-4">
            <button class="bg-transparent border border-capsource-orange text-capsource-orange px-4 py-2 rounded-full text-sm font-medium hover:bg-capsource-orange hover:text-white transition-colors">
              REGISTER
            </button>
            <button class="bg-transparent border border-capsource-orange text-capsource-orange px-4 py-2 rounded-full text-sm font-medium hover:bg-capsource-orange hover:text-white transition-colors">
              LOGIN
            </button>
          </div>

          <!-- Mobile menu button -->
          <div class="md:hidden">
            <button type="button" class="text-gray-700 hover:text-capsource-orange">
              <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
              </svg>
            </button>
          </div>
        </div>
      </div>
    </nav>

    <div class="min-h-screen">
      <%= yield %>
    </div>
  </body>
</html>
