# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# Temporary files generated by your text editor or operating system
# belong in git's global ignore instead:
# `$XDG_CONFIG_HOME/git/ignore` or `~/.config/git/ignore`

# Ignore bundler config.
/.bundle

# Ignore all environment files (SECURITY CRITICAL).
/.env*
!/.env.example

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore pidfiles, but keep the directory.
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# Ignore storage (uploaded files in development and any SQLite databases).
/storage/*
!/storage/.keep
/tmp/storage/*
!/tmp/storage/
!/tmp/storage/.keep

/public/assets

# Ignore master key for decrypting credentials and more (SECURITY CRITICAL).
/config/master.key

/app/assets/builds/*
!/app/assets/builds/.keep

# Ignore uploaded files
/public/uploads/*

# ============================================
# SECURITY: Additional sensitive file patterns
# ============================================

# API Keys and Secrets
*.key
*.pem
*.p12
*.pfx
*.cer
*.crt
*.der
*.p7b
*.p7c

# Credentials and configuration
/config/credentials/*.key
/config/secrets.yml
/config/database.yml.local
credentials.yml
secrets.yml

# Kamal secrets (even though it references ENV, keep it safe)
/.kamal/secrets-*
/.kamal/*.env

# Docker secrets
docker-compose.override.yml
docker-compose.local.yml
.dockerenv

# Cloud provider credentials
.gcloud/
.aws/
.azure/
gcloud-service-key.json
service-account*.json
*-service-account.json

# Database dumps and backups
*.sql
*.sqlite3
*.db
*.dump
/db/*.sqlite3
/db/*.sqlite3-*

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Test coverage and reports
/coverage/
/coverage.data
.coverage
htmlcov/

# Backup files
*.bak
*.backup
*.old
*.orig

# Temporary analysis files
tmp/
*.tmp

# Node modules (if using any JS tools)
node_modules/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Thumbnails
._*

# Yarn (if used)
yarn-error.log
